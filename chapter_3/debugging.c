/*
* Space in between function name and output
* Parentheses instead of bracket
* INT is in capital letters
* First comment is not closed
* Second comment is not closed either
* Space in between printf function and parentheses
* The variable sum should have a comma before it
* The return statement should be on a different line
* No closed bracket!
*/

#include <stdio.h>
int main(void)

{
	int sum;
/* COMPUTE RESULT */
	sum = 25 + 37 - 19;
/* DISPLAY RESULTS */
printf("The answer is %i\n", sum);
return 0;
}
