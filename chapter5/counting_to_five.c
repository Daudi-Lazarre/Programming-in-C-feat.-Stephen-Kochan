#include <stdio.h>

int main(void)
{
	int count = 1;
// While count is less than or equal to the number five,
	while (count <= 5)
	{
// Print the the number and increment it.
		printf ("%i\n", count); ++count;
	}
	return(0);
}
